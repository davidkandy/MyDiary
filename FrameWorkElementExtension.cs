using System.Threading.Tasks;
using System.Windows;

namespace MyDiary
{
    public static class FrameWorkElementExtension
    {
        public static async Task SlideInFromLeftAsync(this FrameworkElement element, AnimationSlideInDirection direction, bool firstLoad, float seconds = 0.3f, bool keepMargin = true, int size = 0)
        {
            
        }
    }
}
